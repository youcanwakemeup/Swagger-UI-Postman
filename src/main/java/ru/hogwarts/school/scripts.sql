SELECT * FROM student WHERE age BETWEEN 10 AND 15;

SELECT name FROM student;

SELECT * FROM student
WHERE UPPER(name) LIKE '%E%';

SELECT * FROM student
WHERE age < id;

SELECT * FROM student
ORDER BY age;


